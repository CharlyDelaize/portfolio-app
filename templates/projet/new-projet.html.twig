{% extends 'base.html.twig' %}

{% block stylesheets %}
<head>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-BmbxuPwQa2lc/FVzBcNJ7UAyJxM6wuqIj61tLrc4wSX0szH/Ev+nYRRuWlolflfl" crossorigin="anonymous">
    <link rel="stylesheet" href="{{ asset('css/new-projet.css') }}">
</head>
{% endblock %}
{% block body %}
<body>
    <header>
        <nav class="navbar">
            {% if app.user %}
                <div class="mb-3">
                    {{ app.user.username }}
                </div>
            {% else %}
                <meta http-equiv="refresh" content="durée=0.1s;URL=login">
            {% endif %}
            <h1>Admin</h1>
            <a href="logout">Déconnexion</a>
        </nav>
    </header>
    <div class="formulaire">
        {{ form_start(formulaire) }}
            {{ form_row(formulaire.nom) }}
            {{ form_row(formulaire.image) }}
            <button type="submit" class="btn btn-primary" id="bouton">Envoyer</button>
        {{ form_end(formulaire) }}
        <button type="button" class="btn btn-primary"><a href="/read-all-projet" id="voirProjet">Voir mes projets</a></button>
    </div>
    <footer>
        &copy; 2021, Charly Delaize
    </footer>
</body>
{% endblock %}