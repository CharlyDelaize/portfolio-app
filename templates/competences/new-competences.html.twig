<div class="formulaire">
    {{ form_start(formulaire) }}
        {{ form_row(formulaire.titre) }}
        {{ form_row(formulaire.image) }}<input type="file">
        <button type="submit" class="btn btn-primary">Envoyer</button>
    {{ form_end(formulaire) }}
</div>